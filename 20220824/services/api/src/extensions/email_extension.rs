use common::email::Email;

pub struct EmailExtension {
    email: 
}

impl EmailExtension {

    pub fn new(email: Email) -> Self {
        return EmailExtension {
            email: email
        };
    }
}